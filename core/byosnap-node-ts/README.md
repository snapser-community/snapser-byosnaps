# BYOSnap Core - Node.js TypeScript

A minimal starter scaffold for a new Node.js + TypeScript BYOSnap. Every endpoint
Snapser expects is present, but each handler body is a STUB that returns a simple
placeholder and carries a `// TODO` describing what you would implement.

Fill in the stubs with your own logic. For a complete, working reference
(Storage Snap integration, import/export, validation, business logic) see
`advanced/byosnap-node-ts`.

This scaffold demonstrates:

- **Configuration Tools** (UI Builder + Custom HTML) — stubbed
- **Import/Export** (Snapend Sync/Clone) — stubbed
- **User Manager Tool** (Custom HTML) — stubbed
- **GDPR User Data** (Get/Update/Delete) — stubbed
- **Five example business endpoints** covering each Snapser auth type
  (user, api-key, internal), a multi-auth endpoint, and an Admin SDK endpoint

## Project Structure

```
byosnap-node-ts/
├── package.json
├── tsconfig.json
├── tsoa.json                    # TSOA configuration for route & swagger generation
├── Dockerfile
├── generate_swagger.sh
├── scripts/
│   └── fix-swagger.ts           # Post-processes generated swagger.json
├── src/
│   ├── server.ts                # Entry point (imports controllers so TSOA registers routes)
│   ├── app.ts                   # Express app setup (CORS, body parser, health check)
│   ├── routes.ts                # Auto-generated by TSOA (do not edit)
│   ├── middleware/
│   │   └── authMiddleware.ts    # Snapser auth validation (user / api-key / internal)
│   ├── types/
│   │   └── responses.ts         # Shared response types
│   ├── models/
│   │   ├── settingsModel.ts     # Settings schema & defaults
│   │   ├── exportSettingsModel.ts
│   │   └── customSettingsModel.ts
│   └── controllers/
│       ├── settingsController.ts  # System endpoints: config, import/export, user manager, GDPR
│       └── exampleController.ts   # Five example business endpoints (one per auth exposure)
└── snapser-resources/
    ├── snapser-byosnap-profile.json
    ├── snapser-snapend-manifest.json
    ├── snapser-base-snapend-manifest.json
    ├── snapser-tool-characters.json
    ├── _complex-snapser-tool-characters.json
    ├── snapser-config-tool-characters-custom.html
    ├── snapser-user-tool-characters-custom.html
    └── .env
```

## Endpoints

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| GET | `/v1/byosnap-core/settings` | internal | Get settings (UI Builder) |
| PUT | `/v1/byosnap-core/settings` | internal | Update settings (UI Builder) |
| GET | `/v1/byosnap-core/settings/custom` | internal | Get custom settings (Custom HTML) |
| PUT | `/v1/byosnap-core/settings/custom` | internal | Update custom settings (Custom HTML) |
| GET | `/v1/byosnap-core/settings/export` | internal | Export all settings |
| POST | `/v1/byosnap-core/settings/import` | internal | Import settings |
| POST | `/v1/byosnap-core/settings/validate-import` | internal | Validate import data |
| GET | `/v1/byosnap-core/settings/users/{userId}/custom` | internal | Get user data (User Manager) |
| POST | `/v1/byosnap-core/settings/users/{userId}/custom` | internal | Update user data (User Manager) |
| GET | `/v1/byosnap-core/settings/users/{userId}/data` | internal | Get user data (GDPR) |
| PUT | `/v1/byosnap-core/settings/users/{userId}/data` | internal | Update user data (GDPR) |
| DELETE | `/v1/byosnap-core/settings/users/{userId}/data` | internal | Delete user data (GDPR) |
| GET | `/v1/byosnap-core/users/{userId}/example` | user | Example: User auth |
| GET | `/v1/byosnap-core/example/api-key` | api-key | Example: Api-Key auth |
| GET | `/v1/byosnap-core/example/internal` | internal | Example: Internal auth |
| GET | `/v1/byosnap-core/example/admin` | admin | Example: Admin SDK (surfaces in Admin SDK; still arrives via internal gateway) |
| GET | `/v1/byosnap-core/users/{userId}/example/multi-auth` | user, api-key, internal | Example: Multi auth (one route, multiple auth types) |
| GET | `/healthz` | none | Health check |

## Development

```bash
# Install dependencies
npm install

# Run in development mode
npm run dev

# Build (compiles TypeScript and regenerates swagger)
npm run build

# Regenerate the tsoa routes and swagger spec
npm run build:routes
npm run build:swagger

# Docker build
docker build -t byosnap-node-ts-core .
```

## Next Steps

Each handler in `settingsController.ts` and `exampleController.ts` is a stub with
a `// TODO`. When you are ready to persist data, call other Snaps, or wire up the
configuration/import-export tooling, look at the matching handler in
`advanced/byosnap-node-ts` for a complete reference (including the generated
`snapser-internal` Storage SDK usage).
